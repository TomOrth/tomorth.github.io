# Thomas Orth — Portfolio

A minimalist editorial portfolio for Senior AI Engineer Thomas Orth.

## Development

```bash
npm install
npm run dev
```

Run `npm run build` to create the production site in `dist/`.

The site deploys to GitHub Pages through `.github/workflows/deploy.yml` and
retains the custom domain configured in `CNAME`.
